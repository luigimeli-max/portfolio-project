# Django Settings
# Genera una SECRET_KEY sicura con: python -c "from django.core.management.utils import get_random_secret_key; print(get_random_secret_key())"
SECRET_KEY=your-super-secret-key-change-this-in-production

# Debug mode - MUST be False in production!
DEBUG=False

# Allowed hosts (comma-separated, no spaces)
ALLOWED_HOSTS=luigimeli.work,www.luigimeli.work,38.242.208.240,localhost,127.0.0.1

# Database (PostgreSQL per produzione)
# Lascia vuoto DB_NAME per usare SQLite in sviluppo
DB_NAME=portfolio_db
DB_USER=portfolio_user
DB_PASSWORD=your-strong-database-password-here
DB_HOST=localhost
DB_PORT=5432

# Email Configuration (opzionale - per form contatti)
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USE_TLS=True
EMAIL_HOST_USER=tua-email@gmail.com
EMAIL_HOST_PASSWORD=tua-password-app-specifica
DEFAULT_FROM_EMAIL=noreply@luigimeli.work
